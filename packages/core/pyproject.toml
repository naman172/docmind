[project]
name = "docmind-core"
version = "0.1.0"
description = "Shared models, LLM client, and retriever logic"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.4",
]

[tool.setuptools.packages.find]
where = ["src"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

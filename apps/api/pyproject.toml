[project]
name = "docmind-api"
version = "0.1.0"
description = "FastAPI service"
requires-python = ">=3.12"
dependencies = [
    "docmind-core",
    "fastapi>=0.136.1",
    "uvicorn>=0.47.0",
]

[tool.uv.sources]
docmind-core = { workspace = true }
